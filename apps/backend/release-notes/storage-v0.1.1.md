## Release Notes

Storage links now use the current Internet Computer gateway domain,
`icp.net`.

Existing `icp0.io` storage links remain compatible, so previously shared
links continue to work while new links use the updated domain.
