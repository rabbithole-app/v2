{
  "name": "treasury-mo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/treasury-mo",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/treasury-mo",
        "command": "docker compose exec -T replica bash -lc 'dfx build treasury'",
        "parallel": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/treasury-mo",
        "command": "vitest",
        "parallel": false
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/treasury-mo",
        "command": "docker compose exec -T replica bash -lc 'lintoko'",
        "parallel": false
      }
    }
  }
}
