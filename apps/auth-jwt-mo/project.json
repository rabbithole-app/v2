{
  "name": "auth-jwt-mo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/auth-jwt-mo",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/auth-jwt-mo",
        "command": "dfx build example",
        "parallel": false
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/auth-jwt-mo",
        "command": "lintoko",
        "parallel": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/auth-jwt-mo",
        "command": "vitest",
        "parallel": false
      }
    }
  }
}
