# encrypted-storage

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build encrypted-storage` to build the library.

## Running unit tests

Run `nx test encrypted-storage` to execute the unit tests via [Vitest](https://vitest.dev/).
